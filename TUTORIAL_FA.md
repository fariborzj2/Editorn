# مستندات توسعه Editron (فاز ۱ تا ۱۷)

این مستندات مراحل توسعه ویرایشگر متن **Editron** را شرح می‌دهد.

---

## ۱. مقدمه (Introduction)
Editron یک ویرایشگر متن مدرن و بلاک‌بیس است. تا کنون ۱۷ فاز توسعه تکمیل شده است.

---

## ۲. ساختار پروژه (Project Structure)
```bash
src/
├── blocks/          # بلاک‌ها
├── core/            # هسته اصلی
│   ├── I18n.ts      # مدیریت چندزبانی
│   ├── Editron.ts   # کلاس اصلی
│   └── ...
├── locales/         # فایل‌های ترجمه
├── plugins/         # پلاگین‌ها
├── utils/           # ابزارها
└── index.ts
```

---

## ۳. قابلیت‌های فاز ۱۷ (Uploads & Advanced Tutorials)

### ۳.۱. آپلود تصویر (Image Upload)
بلاک تصویر اکنون از آپلود فایل پشتیبانی می‌کند.
- **Drag & Drop:** قابلیت کشیدن و رها کردن فایل تصویر.
- **File Input:** انتخاب فایل از سیستم.
- **Custom Handler:** امکان تعریف متد `onImageUpload` در کانفیگ برای اتصال به سرور واقعی.
- **Fallback:** تبدیل خودکار به Base64 در صورت عدم وجود هندلر (برای دمو).

### ۳.۲. آموزش ساخت پلاگین سفارشی (Custom Plugin Tutorial)
در ادامه نحوه ساخت یک پلاگین "Timestamp" را آموزش می‌دهیم.

---

## ۴. آموزش: چگونه یک پلاگین بسازیم؟ (How to Create a Plugin)

برای ساخت یک پلاگین جدید در Editron مراحل زیر را دنبال کنید. فرض کنید می‌خواهیم پلاگینی بسازیم که تاریخ و ساعت جاری را در محل کرسر درج کند.

### گام ۱: تعریف کلاس پلاگین
در پوشه `src/plugins/` یک فایل جدید به نام `Timestamp.ts` بسازید.

```typescript
import { IPlugin } from '../types';
import { Editron } from '../core/Editron';

export class TimestampPlugin implements IPlugin {
    public name: string = 'timestamp';
    private editor: Editron | null = null;

    init(editor: Editron) {
        this.editor = editor;
        // اضافه کردن دکمه یا شورت‌کات
        this.addShortcut();
    }

    addShortcut() {
        this.editor?.holder.addEventListener('keydown', (e) => {
            // Ctrl + Alt + T
            if (e.ctrlKey && e.altKey && e.key === 't') {
                e.preventDefault();
                this.insertTimestamp();
            }
        });
    }

    insertTimestamp() {
        const date = new Date().toLocaleString();
        // استفاده از BlockManager برای اضافه کردن بلاک جدید یا درج در متن
        // اینجا یک بلاک پاراگراف جدید اضافه می‌کنیم
        this.editor?.blockManager.addBlock('paragraph', { text: `📅 ${date}` }, true);
    }
}
```

### گام ۲: ثبت پلاگین
در فایل `src/core/PluginManager.ts` (یا جایی که پلاگین‌ها رجیستر می‌شوند) آن را اضافه کنید.
*نکته: در معماری فعلی ما، پلاگین‌ها به صورت داخلی رجیستر شده‌اند. برای نسخه بعدی، متد `registerPlugin` عمومی اضافه خواهیم کرد.*

---

## ۵. قابلیت‌های قبلی
- **Global:** i18n, RTL.
- **Features:** Checklist, Slash Menu, Inline Toolbar, Autosave.
- **Core:** Event System, History, Sanitizer.

---

## ۶. وضعیت فعلی
- ✅ Core Engine & History
- ✅ All Features & Plugins
- ✅ UX Polish (A11y, Mobile, RTL)
- ✅ **Advanced Features (Uploads)**
- ✅ **Educational Content (Plugin Tutorial)**
- 🚀 **Final Release (V1.4)**
